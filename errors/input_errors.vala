namespace IotaLibVala
{
    public errordomain InputError
    {
        INVALID_TRYTES,
        INVALID_SEED,
        INVALID_CHECKSUM,
        INVALID_ATTACHED_TRYTES,
        INVALID_TRANSFERS,
        INVALID_KEY,
        INVALID_TRUNK_OR_BRANCH,
        INVALID_URI,
        NOT_INT,
        INVALID_INPUTS
    }
}